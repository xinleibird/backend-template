import React, { FC } from 'react';
import { Switch } from 'react-router-dom';

const Routes: FC = () => {
  return <Switch>{/* <Route exact path="/test" component={AsyncLoader('Test')} /> */}</Switch>;
};

export default Routes;
