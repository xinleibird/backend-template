import React, { FC } from 'react';
import { Switch } from 'react-router-dom';

const VisualizeScreenRoutes: FC = () => {
  return (
    <>
      <Switch>{/* <Route exact path="/test" component={AsyncLoader('Test')} /> */}</Switch>
      <Switch>{/* <Route exact path="/test" component={AsyncLoader('Test')} /> */}</Switch>
      <Switch>{/* <Route exact path="/test" component={AsyncLoader('Test')} /> */}</Switch>
    </>
  );
};

export default VisualizeScreenRoutes;
